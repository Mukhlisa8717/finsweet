/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["media.istockphoto.com", "cdn.dummyjson.com", "images.pexels.com", "cdn.dummyjson.com/"]
    }
};

export default nextConfig;